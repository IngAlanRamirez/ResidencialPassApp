# Rediseño UI/UX — ResidencialPassApp Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reemplazar toda la capa visual de ResidencialPassApp con el Design System RP (paleta Terracota, Plus Jakarta Sans, componentes custom) sin tocar servicios, guards ni interceptores.

**Architecture:** CSS custom properties como design tokens centralizados en `variables.scss`, componentes standalone Angular con `@HostBinding` para encapsulación de estilos, Ionic usado únicamente como shell (IonPage/IonContent/IonHeader para safe areas). Dark mode vía `@media (prefers-color-scheme: dark)` sin lógica Angular.

**Tech Stack:** Angular 20 Standalone + Signals, Ionic 8 (shell only), Capacitor 8, Karma/Jasmine, Plus Jakarta Sans (Google Fonts), CSS custom properties.

---

## File Map

### Nuevos
```
src/theme/variables.scss                          — CSS tokens RP (paleta, radios, sombras)
src/theme/typography.scss                         — @import Plus Jakarta Sans + escala tipográfica

src/app/shared/components/rp-button/
  rp-button.component.ts                          — Standalone, @Input variant/loading/disabled
  rp-button.component.html                        — <button> nativo con slot ng-content
  rp-button.component.scss                        — Estilos encapsulados usando tokens RP

src/app/shared/components/rp-input/
  rp-input.component.ts                           — Standalone, ControlValueAccessor, NG_VALUE_ACCESSOR
  rp-input.component.html                         — Label flotante, toggle password, mensaje error
  rp-input.component.scss                         — Focus ring, borde error, min-height 56px

src/app/shared/components/rp-card/
  rp-card.component.ts                            — Standalone, @Input variant: 'action'|'list-item'
  rp-card.component.html                          — Slots: icon, content, action/arrow
  rp-card.component.scss                          — Border-radius según variante

src/app/shared/components/rp-badge/
  rp-badge.component.ts                           — Standalone, @Input status/role
  rp-badge.component.html                         — <span> con texto semántico
  rp-badge.component.scss                         — Colores por status usando @HostBinding
```

### Modificados
```
src/index.html                                    — <link> Google Fonts Plus Jakarta Sans
src/global.scss                                   — Eliminar dark.always.css, importar tokens + typography, @media dark
src/app/features/auth/auth-welcome/auth-welcome.page.html  — Rediseño completo
src/app/features/auth/auth-welcome/auth-welcome.page.scss  — Estilos hero
src/app/features/auth/login/login.page.html       — Swap ion-* por rp-*
src/app/features/auth/login/login.page.scss       — Estilos con tokens
src/app/features/auth/register/register.page.ts   — Signal currentStep<1|2|3>
src/app/features/auth/register/register.page.html — Vista condicional por paso + progress bar
src/app/features/auth/register/register.page.scss — Estilos con tokens
src/app/features/home/home.page.ts                — Añadir API calls para stats admin
src/app/features/home/home.page.html              — Dual Zone admin + layouts por rol
src/app/features/home/home.page.scss              — Estilos Action-First
src/app/features/home/nueva-visita/nueva-visita.page.html — Toggle chips horario
src/app/features/home/nueva-visita/nueva-visita.page.scss
src/app/features/home/detalle-visita/detalle-visita.page.html — QR grande + compartir
src/app/features/home/detalle-visita/detalle-visita.page.scss
src/app/features/home/escanear-visita/escanear-visita.page.html — Dark UI + viewfinder
src/app/features/home/escanear-visita/escanear-visita.page.scss
src/app/features/home/historial-visitas/historial-visitas.page.html — Filtro segmentado + borde lateral
src/app/features/home/historial-visitas/historial-visitas.page.scss
```

---

## Task 1: Foundation — Tokens, Tipografía y Dark Mode

**Files:**
- Modify: `src/index.html`
- Create: `src/theme/variables.scss`
- Create: `src/theme/typography.scss`
- Modify: `src/global.scss`

- [ ] **Step 1: Añadir Google Fonts en index.html**

Abrí `src/index.html` y agregá estas líneas en el `<head>`, antes del `<title>`:

```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;600;700;800&display=swap" rel="stylesheet">
```

- [ ] **Step 2: Crear src/theme/variables.scss**

```scss
// ===================================================
// Design Tokens — Residencial Pass (Paleta Terracota)
// ===================================================

:root {
  // Color base
  --rp-stone-950: #1c1917;
  --rp-stone-700: #44403c;
  --rp-stone-500: #78716c;
  --rp-stone-300: #a8a29e;
  --rp-stone-100: #e7e5e4;

  // Primario
  --rp-primary: #ea580c;
  --rp-primary-dark: #c2410c;
  --rp-primary-tint: #fff7ed;

  // Semánticos
  --rp-success: #16a34a;
  --rp-success-bg: #f0fdf4;
  --rp-success-border: #bbf7d0;
  --rp-danger: #dc2626;
  --rp-danger-bg: #fef2f2;
  --rp-danger-border: #fecaca;

  // Fondos y superficies
  --rp-bg: #fafaf9;
  --rp-surface: #ffffff;

  // Tipografía
  --rp-font: 'Plus Jakarta Sans', system-ui, sans-serif;

  // Radios
  --rp-radius-sm: 6px;
  --rp-radius-md: 10px;
  --rp-radius-lg: 14px;
  --rp-radius-xl: 18px;
  --rp-radius-full: 9999px;

  // Sombras
  --rp-shadow-card: 0 2px 12px rgba(0, 0, 0, 0.08);
  --rp-shadow-button: 0 4px 12px rgba(234, 88, 12, 0.3);

  // Espaciado
  --rp-page-padding: 20px;
}

@media (prefers-color-scheme: dark) {
  :root {
    --rp-bg: #111110;
    --rp-surface: #1c1917;
  }
}
```

- [ ] **Step 3: Crear src/theme/typography.scss**

```scss
// ===================================================
// Escala tipográfica — Plus Jakarta Sans
// ===================================================

.rp-display {
  font-family: var(--rp-font);
  font-size: 26px;
  font-weight: 800;
  line-height: 1.2;
  letter-spacing: -0.5px;
}

.rp-heading {
  font-family: var(--rp-font);
  font-size: 18px;
  font-weight: 700;
  line-height: 1.3;
}

.rp-label {
  font-family: var(--rp-font);
  font-size: 14px;
  font-weight: 600;
  line-height: 1.4;
}

.rp-body {
  font-family: var(--rp-font);
  font-size: 13px;
  font-weight: 400;
  line-height: 1.5;
}

.rp-caption {
  font-family: var(--rp-font);
  font-size: 11px;
  font-weight: 400;
  line-height: 1.4;
}
```

- [ ] **Step 4: Modificar src/global.scss**

Localizá la línea que importa `dark.always.css` y eliminala. El resultado del bloque de imports al inicio debe quedar así:

```scss
/* Core CSS required for Ionic components to work properly */
@import "@ionic/angular/css/core.css";

/* Basic CSS for apps built with Ionic */
@import "@ionic/angular/css/normalize.css";
@import "@ionic/angular/css/structure.css";
@import "@ionic/angular/css/typography.css";
@import "@ionic/angular/css/display.css";

/* Design System RP */
@import "./theme/variables";
@import "./theme/typography";

/* Reset de fuente global */
* {
  font-family: var(--rp-font);
  box-sizing: border-box;
}

ion-content {
  --background: var(--rp-bg);
}
```

> ⚠️ NO importes `@ionic/angular/css/palettes/dark.always.css`. Esa línea es la que fuerza dark siempre. El dark mode ahora viene del `@media` en `variables.scss`.

- [ ] **Step 5: Correr la app y verificar que la fuente cambió**

```bash
ionic serve
```

Expected: La fuente cambia a Plus Jakarta Sans. El fondo de las páginas usa `#fafaf9` en light y `#111110` en dark según preferencia del sistema.

- [ ] **Step 6: Commit**

```bash
git add src/index.html src/theme/variables.scss src/theme/typography.scss src/global.scss
git commit -m "feat(design-system): add RP tokens, Plus Jakarta Sans, and system dark mode"
```

---

## Task 2: RpButtonComponent

**Files:**
- Create: `src/app/shared/components/rp-button/rp-button.component.ts`
- Create: `src/app/shared/components/rp-button/rp-button.component.html`
- Create: `src/app/shared/components/rp-button/rp-button.component.scss`
- Test: `src/app/shared/components/rp-button/rp-button.component.spec.ts`

- [ ] **Step 1: Escribir el test que falla**

Creá `src/app/shared/components/rp-button/rp-button.component.spec.ts`:

```typescript
import { ComponentFixture, TestBed } from '@angular/core/testing';
import { By } from '@angular/platform-browser';
import { RpButtonComponent } from './rp-button.component';

describe('RpButtonComponent', () => {
  let fixture: ComponentFixture<RpButtonComponent>;
  let component: RpButtonComponent;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [RpButtonComponent],
    }).compileComponents();
    fixture = TestBed.createComponent(RpButtonComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should have class rp-btn--primary by default', () => {
    const btn = fixture.debugElement.query(By.css('button'));
    expect(btn.nativeElement.classList).toContain('rp-btn--primary');
  });

  it('should have class rp-btn--secondary when variant is secondary', () => {
    component.variant = 'secondary';
    fixture.detectChanges();
    const btn = fixture.debugElement.query(By.css('button'));
    expect(btn.nativeElement.classList).toContain('rp-btn--secondary');
  });

  it('should be disabled when loading is true', () => {
    component.loading = true;
    fixture.detectChanges();
    const btn = fixture.debugElement.query(By.css('button'));
    expect(btn.nativeElement.disabled).toBeTrue();
  });

  it('should show spinner when loading is true', () => {
    component.loading = true;
    fixture.detectChanges();
    const spinner = fixture.debugElement.query(By.css('.rp-btn__spinner'));
    expect(spinner).toBeTruthy();
  });
});
```

- [ ] **Step 2: Correr el test para verificar que falla**

```bash
npx ng test --include="**/rp-button/**" --watch=false
```

Expected: FAILED — `RpButtonComponent` not found.

- [ ] **Step 3: Implementar el componente**

Creá `src/app/shared/components/rp-button/rp-button.component.ts`:

```typescript
import { Component, Input } from '@angular/core';
import { CommonModule } from '@angular/common';

export type RpButtonVariant = 'primary' | 'secondary' | 'danger';

@Component({
  selector: 'rp-button',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './rp-button.component.html',
  styleUrls: ['./rp-button.component.scss'],
})
export class RpButtonComponent {
  @Input() variant: RpButtonVariant = 'primary';
  @Input() loading = false;
  @Input() disabled = false;
  @Input() type: 'button' | 'submit' = 'button';
}
```

Creá `src/app/shared/components/rp-button/rp-button.component.html`:

```html
<button
  [type]="type"
  [disabled]="disabled || loading"
  [class]="'rp-btn rp-btn--' + variant"
>
  @if (loading) {
    <span class="rp-btn__spinner"></span>
  } @else {
    <ng-content />
  }
</button>
```

Creá `src/app/shared/components/rp-button/rp-button.component.scss`:

```scss
:host {
  display: block;
  width: 100%;
}

.rp-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  height: 48px;
  border: none;
  border-radius: 12px;
  font-family: var(--rp-font);
  font-size: 15px;
  font-weight: 700;
  cursor: pointer;
  transition: opacity 0.15s ease, transform 0.1s ease;
  padding: 0 20px;
  -webkit-tap-highlight-color: transparent;

  &:active:not(:disabled) {
    transform: scale(0.97);
  }

  &:disabled {
    opacity: 0.5;
    cursor: not-allowed;
  }

  &--primary {
    background: var(--rp-primary);
    color: white;
  }

  &--secondary {
    background: var(--rp-surface);
    color: var(--rp-stone-950);
    border: 1.5px solid var(--rp-stone-100);
  }

  &--danger {
    background: var(--rp-danger-bg);
    color: var(--rp-danger);
    border: 1.5px solid var(--rp-danger-border);
  }
}

.rp-btn__spinner {
  width: 20px;
  height: 20px;
  border: 2px solid rgba(255, 255, 255, 0.3);
  border-top-color: white;
  border-radius: 50%;
  animation: rp-spin 0.7s linear infinite;
}

@keyframes rp-spin {
  to { transform: rotate(360deg); }
}
```

- [ ] **Step 4: Correr los tests y verificar que pasan**

```bash
npx ng test --include="**/rp-button/**" --watch=false
```

Expected: PASSED — 4 tests passing.

- [ ] **Step 5: Commit**

```bash
git add src/app/shared/components/rp-button/
git commit -m "feat(rp-button): add standalone button component with variants and loading state"
```

---

## Task 3: RpInputComponent (ControlValueAccessor)

**Files:**
- Create: `src/app/shared/components/rp-input/rp-input.component.ts`
- Create: `src/app/shared/components/rp-input/rp-input.component.html`
- Create: `src/app/shared/components/rp-input/rp-input.component.scss`
- Test: `src/app/shared/components/rp-input/rp-input.component.spec.ts`

- [ ] **Step 1: Escribir el test que falla**

Creá `src/app/shared/components/rp-input/rp-input.component.spec.ts`:

```typescript
import { ComponentFixture, TestBed } from '@angular/core/testing';
import { By } from '@angular/platform-browser';
import { FormControl, ReactiveFormsModule } from '@angular/forms';
import { Component } from '@angular/core';
import { RpInputComponent } from './rp-input.component';

@Component({
  standalone: true,
  imports: [RpInputComponent, ReactiveFormsModule],
  template: `<rp-input label="Email" [formControl]="ctrl" />`
})
class TestHost {
  ctrl = new FormControl('');
}

describe('RpInputComponent', () => {
  let fixture: ComponentFixture<TestHost>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [TestHost],
    }).compileComponents();
    fixture = TestBed.createComponent(TestHost);
    fixture.detectChanges();
  });

  it('should render label', () => {
    const label = fixture.debugElement.query(By.css('.rp-input__label'));
    expect(label.nativeElement.textContent).toContain('Email');
  });

  it('should update form control value on input', () => {
    const host = fixture.componentInstance;
    const input = fixture.debugElement.query(By.css('input'));
    input.nativeElement.value = 'test@example.com';
    input.nativeElement.dispatchEvent(new Event('input'));
    fixture.detectChanges();
    expect(host.ctrl.value).toBe('test@example.com');
  });

  it('should show error class when errorMessage is provided', () => {
    const rp = fixture.debugElement.query(By.css('rp-input'));
    rp.componentInstance.errorMessage = 'Campo requerido';
    fixture.detectChanges();
    const wrapper = fixture.debugElement.query(By.css('.rp-input__wrapper'));
    expect(wrapper.nativeElement.classList).toContain('rp-input__wrapper--error');
  });
});
```

- [ ] **Step 2: Correr el test para verificar que falla**

```bash
npx ng test --include="**/rp-input/**" --watch=false
```

Expected: FAILED — `RpInputComponent` not found.

- [ ] **Step 3: Implementar el componente**

Creá `src/app/shared/components/rp-input/rp-input.component.ts`:

```typescript
import {
  Component,
  Input,
  forwardRef,
  signal,
} from '@angular/core';
import { CommonModule } from '@angular/common';
import {
  ControlValueAccessor,
  NG_VALUE_ACCESSOR,
  FormsModule,
} from '@angular/forms';

@Component({
  selector: 'rp-input',
  standalone: true,
  imports: [CommonModule, FormsModule],
  templateUrl: './rp-input.component.html',
  styleUrls: ['./rp-input.component.scss'],
  providers: [
    {
      provide: NG_VALUE_ACCESSOR,
      useExisting: forwardRef(() => RpInputComponent),
      multi: true,
    },
  ],
})
export class RpInputComponent implements ControlValueAccessor {
  @Input() label = '';
  @Input() type: 'text' | 'password' | 'tel' | 'email' = 'text';
  @Input() placeholder = '';
  @Input() errorMessage = '';

  value = '';
  disabled = false;
  showPassword = signal(false);

  private onChange: (v: string) => void = () => {};
  private onTouched: () => void = () => {};

  get inputType(): string {
    if (this.type === 'password') {
      return this.showPassword() ? 'text' : 'password';
    }
    return this.type;
  }

  onInput(event: Event): void {
    const val = (event.target as HTMLInputElement).value;
    this.value = val;
    this.onChange(val);
  }

  onBlur(): void {
    this.onTouched();
  }

  togglePassword(): void {
    this.showPassword.update(v => !v);
  }

  writeValue(val: string): void {
    this.value = val ?? '';
  }

  registerOnChange(fn: (v: string) => void): void {
    this.onChange = fn;
  }

  registerOnTouched(fn: () => void): void {
    this.onTouched = fn;
  }

  setDisabledState(isDisabled: boolean): void {
    this.disabled = isDisabled;
  }
}
```

Creá `src/app/shared/components/rp-input/rp-input.component.html`:

```html
<div class="rp-input">
  <div
    class="rp-input__wrapper"
    [class.rp-input__wrapper--error]="errorMessage"
  >
    <label class="rp-input__label">{{ label }}</label>
    <div class="rp-input__row">
      <input
        class="rp-input__field"
        [type]="inputType"
        [placeholder]="placeholder"
        [disabled]="disabled"
        [value]="value"
        (input)="onInput($event)"
        (blur)="onBlur()"
      />
      @if (type === 'password') {
        <button
          type="button"
          class="rp-input__toggle"
          (click)="togglePassword()"
          tabindex="-1"
        >
          {{ showPassword() ? '🙈' : '👁' }}
        </button>
      }
    </div>
  </div>
  @if (errorMessage) {
    <span class="rp-input__error">{{ errorMessage }}</span>
  }
</div>
```

Creá `src/app/shared/components/rp-input/rp-input.component.scss`:

```scss
.rp-input {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.rp-input__wrapper {
  background: var(--rp-surface);
  border: 1.5px solid var(--rp-stone-100);
  border-radius: var(--rp-radius-md);
  min-height: 56px;
  padding: 10px 14px;
  display: flex;
  flex-direction: column;
  justify-content: center;
  transition: border-color 0.15s ease, box-shadow 0.15s ease;

  &:focus-within {
    border-color: var(--rp-primary);
    box-shadow: 0 0 0 3px var(--rp-primary-tint);
  }

  &--error {
    border-color: var(--rp-danger);
    box-shadow: 0 0 0 3px var(--rp-danger-bg);
  }
}

.rp-input__label {
  font-family: var(--rp-font);
  font-size: 10px;
  font-weight: 600;
  color: var(--rp-stone-500);
  text-transform: uppercase;
  letter-spacing: 0.06em;
  margin-bottom: 4px;
}

.rp-input__row {
  display: flex;
  align-items: center;
  gap: 8px;
}

.rp-input__field {
  flex: 1;
  border: none;
  outline: none;
  background: transparent;
  font-family: var(--rp-font);
  font-size: 15px;
  color: var(--rp-stone-950);
  padding: 0;

  &::placeholder {
    color: var(--rp-stone-300);
  }

  &:disabled {
    opacity: 0.5;
  }
}

.rp-input__toggle {
  background: none;
  border: none;
  padding: 4px;
  cursor: pointer;
  font-size: 16px;
  line-height: 1;
  -webkit-tap-highlight-color: transparent;
}

.rp-input__error {
  font-family: var(--rp-font);
  font-size: 12px;
  color: var(--rp-danger);
  padding-left: 2px;
}
```

- [ ] **Step 4: Correr los tests y verificar que pasan**

```bash
npx ng test --include="**/rp-input/**" --watch=false
```

Expected: PASSED — 3 tests passing.

- [ ] **Step 5: Commit**

```bash
git add src/app/shared/components/rp-input/
git commit -m "feat(rp-input): add ControlValueAccessor input with floating label and error state"
```

---

## Task 4: RpCardComponent

**Files:**
- Create: `src/app/shared/components/rp-card/rp-card.component.ts`
- Create: `src/app/shared/components/rp-card/rp-card.component.html`
- Create: `src/app/shared/components/rp-card/rp-card.component.scss`
- Test: `src/app/shared/components/rp-card/rp-card.component.spec.ts`

- [ ] **Step 1: Escribir el test que falla**

```typescript
// src/app/shared/components/rp-card/rp-card.component.spec.ts
import { ComponentFixture, TestBed } from '@angular/core/testing';
import { By } from '@angular/platform-browser';
import { RpCardComponent } from './rp-card.component';

describe('RpCardComponent', () => {
  let fixture: ComponentFixture<RpCardComponent>;
  let component: RpCardComponent;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [RpCardComponent],
    }).compileComponents();
    fixture = TestBed.createComponent(RpCardComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should apply rp-card--action class for action variant', () => {
    component.variant = 'action';
    fixture.detectChanges();
    const card = fixture.debugElement.query(By.css('.rp-card'));
    expect(card.nativeElement.classList).toContain('rp-card--action');
  });

  it('should apply rp-card--list-item class for list-item variant', () => {
    component.variant = 'list-item';
    fixture.detectChanges();
    const card = fixture.debugElement.query(By.css('.rp-card'));
    expect(card.nativeElement.classList).toContain('rp-card--list-item');
  });
});
```

- [ ] **Step 2: Correr el test para verificar que falla**

```bash
npx ng test --include="**/rp-card/**" --watch=false
```

Expected: FAILED.

- [ ] **Step 3: Implementar el componente**

Creá `src/app/shared/components/rp-card/rp-card.component.ts`:

```typescript
import { Component, Input } from '@angular/core';
import { CommonModule } from '@angular/common';

export type RpCardVariant = 'action' | 'list-item';

@Component({
  selector: 'rp-card',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './rp-card.component.html',
  styleUrls: ['./rp-card.component.scss'],
})
export class RpCardComponent {
  @Input() variant: RpCardVariant = 'action';
}
```

Creá `src/app/shared/components/rp-card/rp-card.component.html`:

```html
<div [class]="'rp-card rp-card--' + variant">
  <ng-content />
</div>
```

Creá `src/app/shared/components/rp-card/rp-card.component.scss`:

```scss
.rp-card {
  background: var(--rp-surface);
  box-shadow: var(--rp-shadow-card);

  &--action {
    border-radius: var(--rp-radius-xl);
    padding: 20px 16px;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 12px;
  }

  &--list-item {
    border-radius: var(--rp-radius-lg);
    padding: 14px 16px;
    display: flex;
    align-items: center;
    gap: 12px;
  }
}
```

- [ ] **Step 4: Correr los tests y verificar que pasan**

```bash
npx ng test --include="**/rp-card/**" --watch=false
```

Expected: PASSED.

- [ ] **Step 5: Commit**

```bash
git add src/app/shared/components/rp-card/
git commit -m "feat(rp-card): add standalone card component with action and list-item variants"
```

---

## Task 5: RpBadgeComponent

**Files:**
- Create: `src/app/shared/components/rp-badge/rp-badge.component.ts`
- Create: `src/app/shared/components/rp-badge/rp-badge.component.html`
- Create: `src/app/shared/components/rp-badge/rp-badge.component.scss`
- Test: `src/app/shared/components/rp-badge/rp-badge.component.spec.ts`

- [ ] **Step 1: Escribir el test que falla**

```typescript
// src/app/shared/components/rp-badge/rp-badge.component.spec.ts
import { ComponentFixture, TestBed } from '@angular/core/testing';
import { By } from '@angular/platform-browser';
import { RpBadgeComponent } from './rp-badge.component';

describe('RpBadgeComponent', () => {
  let fixture: ComponentFixture<RpBadgeComponent>;
  let component: RpBadgeComponent;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [RpBadgeComponent],
    }).compileComponents();
    fixture = TestBed.createComponent(RpBadgeComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should show "Pendiente" label for pending status', () => {
    component.status = 'pending';
    fixture.detectChanges();
    const badge = fixture.debugElement.query(By.css('.rp-badge'));
    expect(badge.nativeElement.textContent.trim()).toBe('Pendiente');
  });

  it('should show "Completada" label for finished status', () => {
    component.status = 'finished';
    fixture.detectChanges();
    const badge = fixture.debugElement.query(By.css('.rp-badge'));
    expect(badge.nativeElement.textContent.trim()).toBe('Completada');
  });

  it('should show "Cancelada" label for cancelled status', () => {
    component.status = 'cancelled';
    fixture.detectChanges();
    const badge = fixture.debugElement.query(By.css('.rp-badge'));
    expect(badge.nativeElement.textContent.trim()).toBe('Cancelada');
  });

  it('should apply correct class for each status', () => {
    component.status = 'pending';
    fixture.detectChanges();
    const badge = fixture.debugElement.query(By.css('.rp-badge'));
    expect(badge.nativeElement.classList).toContain('rp-badge--pending');
  });
});
```

- [ ] **Step 2: Correr el test para verificar que falla**

```bash
npx ng test --include="**/rp-badge/**" --watch=false
```

Expected: FAILED.

- [ ] **Step 3: Implementar el componente**

Creá `src/app/shared/components/rp-badge/rp-badge.component.ts`:

```typescript
import { Component, Input } from '@angular/core';
import { CommonModule } from '@angular/common';

export type RpBadgeStatus =
  | 'pending' | 'used' | 'finished' | 'expired' | 'cancelled'
  | 'active' | 'suspended';
export type RpBadgeRole = 'vecino' | 'admin' | 'vigilancia';

const STATUS_LABELS: Record<RpBadgeStatus, string> = {
  pending: 'Pendiente',
  used: 'En curso',
  finished: 'Completada',
  expired: 'Vencida',
  cancelled: 'Cancelada',
  active: 'Activo',
  suspended: 'Suspendido',
};

const ROLE_LABELS: Record<RpBadgeRole, string> = {
  vecino: 'Vecino',
  admin: 'Administrador',
  vigilancia: 'Vigilancia',
};

@Component({
  selector: 'rp-badge',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './rp-badge.component.html',
  styleUrls: ['./rp-badge.component.scss'],
})
export class RpBadgeComponent {
  @Input() status?: RpBadgeStatus;
  @Input() role?: RpBadgeRole;

  get label(): string {
    if (this.status) return STATUS_LABELS[this.status] ?? this.status;
    if (this.role) return ROLE_LABELS[this.role] ?? this.role;
    return '';
  }

  get modifier(): string {
    return this.status ?? this.role ?? '';
  }
}
```

Creá `src/app/shared/components/rp-badge/rp-badge.component.html`:

```html
<span [class]="'rp-badge rp-badge--' + modifier">{{ label }}</span>
```

Creá `src/app/shared/components/rp-badge/rp-badge.component.scss`:

```scss
.rp-badge {
  display: inline-flex;
  align-items: center;
  padding: 3px 10px;
  border-radius: var(--rp-radius-full);
  font-family: var(--rp-font);
  font-size: 11px;
  font-weight: 600;
  border: 1px solid transparent;
  white-space: nowrap;

  // Status
  &--pending, &--used {
    background: var(--rp-primary-tint);
    border-color: #fed7aa;
    color: var(--rp-primary-dark);
  }

  &--finished, &--active {
    background: var(--rp-success-bg);
    border-color: var(--rp-success-border);
    color: #15803d;
  }

  &--cancelled {
    background: var(--rp-danger-bg);
    border-color: var(--rp-danger-border);
    color: var(--rp-danger);
  }

  &--expired, &--suspended {
    background: #f8fafc;
    border-color: #e2e8f0;
    color: #64748b;
  }

  // Roles
  &--vecino {
    background: #eff6ff;
    border-color: #bfdbfe;
    color: #1d4ed8;
  }

  &--admin {
    background: var(--rp-primary-tint);
    border-color: #fed7aa;
    color: var(--rp-primary-dark);
  }

  &--vigilancia {
    background: #f0fdf4;
    border-color: #bbf7d0;
    color: #15803d;
  }
}
```

- [ ] **Step 4: Correr los tests y verificar que pasan**

```bash
npx ng test --include="**/rp-badge/**" --watch=false
```

Expected: PASSED — 4 tests passing.

- [ ] **Step 5: Commit**

```bash
git add src/app/shared/components/rp-badge/
git commit -m "feat(rp-badge): add standalone badge component with semantic status and role colors"
```

---

## Task 6: Auth Welcome — Rediseño

**Files:**
- Modify: `src/app/features/auth/auth-welcome/auth-welcome.page.html`
- Modify: `src/app/features/auth/auth-welcome/auth-welcome.page.scss`
- Modify: `src/app/features/auth/auth-welcome/auth-welcome.page.ts` (agregar imports de componentes RP)

- [ ] **Step 1: Actualizar el componente TS para importar RpButtonComponent**

Abrí `auth-welcome.page.ts`. Agregá `RpButtonComponent` al array `imports` del decorador `@Component`. Si no tenés el array imports, agregalo:

```typescript
import { RpButtonComponent } from '../../../shared/components/rp-button/rp-button.component';

@Component({
  selector: 'app-auth-welcome',
  standalone: true,
  imports: [
    IonContent,
    IonPage,
    RouterLink,
    RpButtonComponent,
    // ... los que ya estén
  ],
  templateUrl: './auth-welcome.page.html',
  styleUrls: ['./auth-welcome.page.scss'],
})
```

- [ ] **Step 2: Reemplazar el template**

Reemplazá el contenido completo de `auth-welcome.page.html`:

```html
<ion-page>
  <ion-content [fullscreen]="true" [scrollY]="false">
    <div class="welcome">

      <!-- Hero -->
      <div class="welcome__hero">
        <div class="welcome__logo">🏘️</div>
        <h1 class="welcome__title">Residencial<br/>Villa María</h1>
        <p class="welcome__tagline">Control de acceso inteligente</p>
      </div>

      <!-- Actions -->
      <div class="welcome__actions">
        <rp-button routerLink="/auth/login">Iniciar sesión</rp-button>
        <rp-button variant="secondary" routerLink="/auth/register">Registrarme</rp-button>
        <p class="welcome__brand">AR Solutions MX</p>
      </div>

    </div>
  </ion-content>
</ion-page>
```

- [ ] **Step 3: Reemplazar los estilos**

Reemplazá el contenido completo de `auth-welcome.page.scss`:

```scss
.welcome {
  display: flex;
  flex-direction: column;
  height: 100%;
}

.welcome__hero {
  flex: 1;
  background: linear-gradient(160deg, #1c1917 0%, #292524 60%, #3c2415 100%);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 48px var(--rp-page-padding) 40px;
  text-align: center;
}

.welcome__logo {
  width: 72px;
  height: 72px;
  background: var(--rp-primary);
  border-radius: 20px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 36px;
  box-shadow: 0 8px 24px rgba(234, 88, 12, 0.4);
  margin-bottom: 20px;
}

.welcome__title {
  font-family: var(--rp-font);
  font-size: 28px;
  font-weight: 800;
  color: white;
  line-height: 1.15;
  margin: 0 0 8px;
}

.welcome__tagline {
  font-family: var(--rp-font);
  font-size: 13px;
  color: var(--rp-stone-300);
  margin: 0;
}

.welcome__actions {
  background: var(--rp-bg);
  padding: 24px var(--rp-page-padding) 40px;
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.welcome__brand {
  font-family: var(--rp-font);
  font-size: 11px;
  color: var(--rp-stone-300);
  text-align: center;
  margin: 4px 0 0;
}
```

- [ ] **Step 4: Verificar en el browser que el diseño es correcto**

```bash
ionic serve
```

Navegá a `/auth/welcome`. Deberías ver: hero oscuro con gradiente, logo naranja con sombra, título blanco, dos botones (naranja y outline), brand al pie.

- [ ] **Step 5: Commit**

```bash
git add src/app/features/auth/auth-welcome/
git commit -m "feat(auth-welcome): redesign welcome screen with RP hero and token-based styles"
```

---

## Task 7: Login — Rediseño

**Files:**
- Modify: `src/app/features/auth/login/login.page.ts`
- Modify: `src/app/features/auth/login/login.page.html`
- Modify: `src/app/features/auth/login/login.page.scss`

- [ ] **Step 1: Actualizar imports en login.page.ts**

Agregá al array `imports` del componente:

```typescript
import { RpButtonComponent } from '../../../shared/components/rp-button/rp-button.component';
import { RpInputComponent } from '../../../shared/components/rp-input/rp-input.component';
```

El array `imports` debe incluir: `IonPage, IonContent, IonHeader, IonToolbar, ReactiveFormsModule, RpButtonComponent, RpInputComponent`. Eliminá `IonInput, IonButton, IonIcon, IonCheckbox` si los había.

- [ ] **Step 2: Reemplazar el template de login.page.html**

```html
<ion-page>

  <!-- Header oscuro -->
  <div class="login-header">
    <button class="login-header__back" (click)="goBack()" type="button">←</button>
    <div>
      <h1 class="login-header__title">Bienvenido</h1>
      <p class="login-header__subtitle">Ingresá tus datos</p>
    </div>
  </div>

  <ion-content [fullscreen]="true">
    <form [formGroup]="loginForm" (ngSubmit)="onSubmit()" class="login-form">

      <rp-input
        label="Teléfono"
        type="tel"
        placeholder="+52 55 1234 5678"
        formControlName="phone"
        [errorMessage]="phoneError()"
      />

      <rp-input
        label="Contraseña"
        type="password"
        placeholder="Tu contraseña"
        formControlName="password"
        [errorMessage]="passwordError()"
      />

      <!-- Recordar contraseña -->
      <label class="login-remember">
        <input
          type="checkbox"
          [checked]="rememberPassword()"
          (change)="rememberPassword.set(!rememberPassword())"
          class="login-remember__checkbox"
        />
        <span class="login-remember__label">Recordar contraseña</span>
      </label>

      <rp-button type="submit" [loading]="loadingSubmit()">
        Ingresar
      </rp-button>

      <button type="button" class="login-forgot">Olvidé mi contraseña</button>

    </form>
  </ion-content>
</ion-page>
```

- [ ] **Step 3: Agregar método goBack() en login.page.ts si no existe**

```typescript
import { Location } from '@angular/common';

// En el constructor o inject:
private location = inject(Location);

goBack(): void {
  this.location.back();
}
```

También agregá las computed signals de errores si no existen:

```typescript
phoneError = computed(() => {
  const ctrl = this.loginForm.get('phone');
  if (ctrl?.invalid && ctrl?.touched) return 'Teléfono requerido';
  return '';
});

passwordError = computed(() => {
  const ctrl = this.loginForm.get('password');
  if (ctrl?.invalid && ctrl?.touched) return 'Contraseña requerida';
  return '';
});
```

- [ ] **Step 4: Reemplazar login.page.scss**

```scss
.login-header {
  background: var(--rp-stone-950);
  padding: 16px var(--rp-page-padding) 24px;
  display: flex;
  align-items: center;
  gap: 12px;
  padding-top: calc(16px + var(--ion-safe-area-top, 0px));
}

.login-header__back {
  width: 36px;
  height: 36px;
  background: rgba(255, 255, 255, 0.1);
  border: none;
  border-radius: 10px;
  color: white;
  font-size: 18px;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.login-header__title {
  font-family: var(--rp-font);
  font-size: 18px;
  font-weight: 800;
  color: white;
  margin: 0 0 2px;
}

.login-header__subtitle {
  font-family: var(--rp-font);
  font-size: 12px;
  color: var(--rp-stone-500);
  margin: 0;
}

.login-form {
  padding: 24px var(--rp-page-padding);
  display: flex;
  flex-direction: column;
  gap: 14px;
}

.login-remember {
  display: flex;
  align-items: center;
  gap: 10px;
  cursor: pointer;
}

.login-remember__checkbox {
  width: 18px;
  height: 18px;
  accent-color: var(--rp-primary);
  cursor: pointer;
}

.login-remember__label {
  font-family: var(--rp-font);
  font-size: 13px;
  color: var(--rp-stone-700);
}

.login-forgot {
  background: none;
  border: none;
  font-family: var(--rp-font);
  font-size: 13px;
  font-weight: 600;
  color: var(--rp-primary);
  text-decoration: underline;
  cursor: pointer;
  align-self: center;
  padding: 4px 0;
}
```

- [ ] **Step 5: Verificar en browser**

```bash
ionic serve
```

Navegá a `/auth/login`. Verificá: header oscuro, dos inputs con label flotante, checkbox recordar, botón naranja, link olvidé contraseña.

- [ ] **Step 6: Commit**

```bash
git add src/app/features/auth/login/
git commit -m "feat(login): redesign login page with rp-input and rp-button components"
```

---

## Task 8: Register — 3 Pasos con Progress Bar

**Files:**
- Modify: `src/app/features/auth/register/register.page.ts`
- Modify: `src/app/features/auth/register/register.page.html`
- Modify: `src/app/features/auth/register/register.page.scss`

- [ ] **Step 1: Actualizar register.page.ts**

Mantenés el FormGroup existente. Agregás `currentStep` signal y los métodos de navegación entre pasos:

```typescript
import { signal, computed } from '@angular/core';
import { RpButtonComponent } from '../../../shared/components/rp-button/rp-button.component';
import { RpInputComponent } from '../../../shared/components/rp-input/rp-input.component';

// Dentro del componente:
currentStep = signal<1 | 2 | 3>(1);

nextStep(): void {
  const step = this.currentStep();
  if (step < 3) this.currentStep.set((step + 1) as 1 | 2 | 3);
}

prevStep(): void {
  const step = this.currentStep();
  if (step > 1) this.currentStep.set((step - 1) as 1 | 2 | 3);
}

step1Valid = computed(() => {
  const form = this.registerForm;
  return form.get('street')?.valid &&
         form.get('streetNumber')?.valid;
});

step2Valid = computed(() => {
  const form = this.registerForm;
  return form.get('phone')?.valid &&
         form.get('password')?.valid &&
         form.get('confirmPassword')?.valid;
});
```

Agregá `RpButtonComponent, RpInputComponent` al array `imports`.

- [ ] **Step 2: Reemplazar register.page.html**

```html
<ion-page>

  <!-- Header oscuro con progress -->
  <div class="register-header">
    <div class="register-header__top">
      <button
        class="register-header__back"
        type="button"
        (click)="currentStep() > 1 ? prevStep() : goBack()"
      >←</button>
      <div>
        <h1 class="register-header__title">Crear cuenta</h1>
        <p class="register-header__subtitle">Tu domicilio en el residencial</p>
      </div>
    </div>
    <!-- Progress bar -->
    <div class="register-progress">
      <div class="register-progress__bar" [class.active]="currentStep() >= 1"></div>
      <div class="register-progress__bar" [class.active]="currentStep() >= 2"></div>
      <div class="register-progress__bar" [class.active]="currentStep() >= 3"></div>
    </div>
  </div>

  <ion-content [fullscreen]="true">
    <form [formGroup]="registerForm" (ngSubmit)="onSubmit()" class="register-form">

      <!-- PASO 1: Domicilio -->
      @if (currentStep() === 1) {
        <p class="register-step-label">PASO 1 DE 3 — DOMICILIO</p>

        <div class="register-address">
          <label class="register-select-wrapper">
            <span class="register-select-label">CALLE</span>
            <select class="register-select" formControlName="street">
              <option value="" disabled>Seleccioná tu calle</option>
              @for (street of streets; track street) {
                <option [value]="street">{{ street }}</option>
              }
            </select>
            <span class="register-select-arrow">⌄</span>
          </label>
        </div>

        <div class="register-row">
          <rp-input
            label="Número"
            type="text"
            placeholder="42"
            formControlName="streetNumber"
          />
          <rp-input
            label="Letra"
            type="text"
            placeholder="Opcional"
            formControlName="streetLetter"
          />
        </div>

        <rp-button type="button" [disabled]="!step1Valid()" (click)="nextStep()">
          Siguiente →
        </rp-button>
      }

      <!-- PASO 2: Acceso -->
      @if (currentStep() === 2) {
        <p class="register-step-label">PASO 2 DE 3 — ACCESO</p>

        <rp-input
          label="Teléfono"
          type="tel"
          placeholder="+52 55 1234 5678"
          formControlName="phone"
        />

        <rp-input
          label="Contraseña"
          type="password"
          placeholder="Mínimo 8 caracteres"
          formControlName="password"
        />

        <rp-input
          label="Confirmar contraseña"
          type="password"
          placeholder="Repetí tu contraseña"
          formControlName="confirmPassword"
        />

        <rp-button type="button" [disabled]="!step2Valid()" (click)="nextStep()">
          Siguiente →
        </rp-button>
      }

      <!-- PASO 3: Confirmación -->
      @if (currentStep() === 3) {
        <p class="register-step-label">PASO 3 DE 3 — CONFIRMACIÓN</p>

        <div class="register-summary">
          <div class="register-summary__item">
            <span class="register-summary__label">Domicilio</span>
            <span class="register-summary__value">
              {{ registerForm.get('street')?.value }} {{ registerForm.get('streetNumber')?.value }}
              @if (registerForm.get('streetLetter')?.value) {
                {{ registerForm.get('streetLetter')?.value }}
              }
            </span>
          </div>
          <div class="register-summary__item">
            <span class="register-summary__label">Teléfono</span>
            <span class="register-summary__value">{{ registerForm.get('phone')?.value }}</span>
          </div>
        </div>

        <rp-button type="submit" [loading]="loadingSubmit()">
          Crear mi cuenta
        </rp-button>
      }

    </form>
  </ion-content>
</ion-page>
```

- [ ] **Step 3: Reemplazar register.page.scss**

```scss
.register-header {
  background: var(--rp-stone-950);
  padding-top: calc(16px + var(--ion-safe-area-top, 0px));
  padding-bottom: 16px;
}

.register-header__top {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 0 var(--rp-page-padding) 14px;
}

.register-header__back {
  width: 36px;
  height: 36px;
  background: rgba(255, 255, 255, 0.1);
  border: none;
  border-radius: 10px;
  color: white;
  font-size: 18px;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.register-header__title {
  font-family: var(--rp-font);
  font-size: 18px;
  font-weight: 800;
  color: white;
  margin: 0 0 2px;
}

.register-header__subtitle {
  font-family: var(--rp-font);
  font-size: 12px;
  color: var(--rp-stone-500);
  margin: 0;
}

.register-progress {
  display: flex;
  gap: 6px;
  padding: 0 var(--rp-page-padding);
}

.register-progress__bar {
  flex: 1;
  height: 3px;
  background: rgba(255, 255, 255, 0.2);
  border-radius: 2px;
  transition: background 0.3s ease;

  &.active {
    background: var(--rp-primary);
  }
}

.register-form {
  padding: 24px var(--rp-page-padding);
  display: flex;
  flex-direction: column;
  gap: 14px;
}

.register-step-label {
  font-family: var(--rp-font);
  font-size: 10px;
  font-weight: 700;
  color: var(--rp-stone-500);
  letter-spacing: 0.08em;
  margin: 0 0 4px;
}

.register-row {
  display: flex;
  gap: 10px;

  rp-input {
    flex: 1;
  }
}

.register-select-wrapper {
  background: var(--rp-surface);
  border: 1.5px solid var(--rp-stone-100);
  border-radius: var(--rp-radius-md);
  min-height: 56px;
  padding: 10px 14px;
  display: flex;
  flex-direction: column;
  position: relative;
}

.register-select-label {
  font-family: var(--rp-font);
  font-size: 10px;
  font-weight: 600;
  color: var(--rp-stone-500);
  text-transform: uppercase;
  letter-spacing: 0.06em;
  margin-bottom: 4px;
}

.register-select {
  border: none;
  outline: none;
  background: transparent;
  font-family: var(--rp-font);
  font-size: 15px;
  color: var(--rp-stone-950);
  appearance: none;
  padding-right: 24px;
}

.register-select-arrow {
  position: absolute;
  right: 14px;
  bottom: 14px;
  color: var(--rp-stone-300);
  pointer-events: none;
}

.register-address {
  display: flex;
  flex-direction: column;
}

.register-summary {
  background: var(--rp-surface);
  border-radius: var(--rp-radius-lg);
  padding: 16px;
  display: flex;
  flex-direction: column;
  gap: 12px;
  box-shadow: var(--rp-shadow-card);
}

.register-summary__item {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.register-summary__label {
  font-family: var(--rp-font);
  font-size: 12px;
  font-weight: 600;
  color: var(--rp-stone-500);
}

.register-summary__value {
  font-family: var(--rp-font);
  font-size: 13px;
  font-weight: 600;
  color: var(--rp-stone-950);
}
```

- [ ] **Step 4: Verificar navegación entre pasos**

```bash
ionic serve
```

Navegá a `/auth/register`. Verificá: progress bar avanza al completar cada paso, back button retrocede entre pasos, paso 3 muestra resumen antes de enviar.

- [ ] **Step 5: Commit**

```bash
git add src/app/features/auth/register/
git commit -m "feat(register): split registration into 3 steps with progress bar"
```

---

## Task 9: Home — Rediseño por Rol (+ Admin Dual Zone)

**Files:**
- Modify: `src/app/features/home/home.page.ts`
- Modify: `src/app/features/home/home.page.html`
- Modify: `src/app/features/home/home.page.scss`

- [ ] **Step 1: Actualizar home.page.ts con imports y stats admin**

Agregá al array `imports`: `RpButtonComponent, RpCardComponent, RpBadgeComponent`.

Agregá las signals para stats del admin (van a llamar a la API si existe, de lo contrario los inicializás en 0 y los poblás cuando la API esté disponible):

```typescript
import { RpButtonComponent } from '../../shared/components/rp-button/rp-button.component';
import { RpCardComponent } from '../../shared/components/rp-card/rp-card.component';
import { RpBadgeComponent } from '../../shared/components/rp-badge/rp-badge.component';

// Dentro del componente, agregá:
adminStats = signal({ activeResidents: 0, pendingRegistrations: 0, guards: 0 });
```

En `ngOnInit()` o `ionViewWillEnter()`, después de cargar el usuario, si es admin, llamá al endpoint de stats. Si no existe un endpoint específico aún, dejá los valores en 0 por ahora — la lógica de servicios no se toca en esta fase.

- [ ] **Step 2: Reemplazar home.page.html**

```html
<ion-page>

  <!-- Header con saludo -->
  <div class="home-header">
    <div class="home-header__greeting">
      <p class="home-header__hello">Hola,</p>
      <h1 class="home-header__name">{{ authState.user()?.firstName ?? 'Vecino' }}</h1>
    </div>
    @if (authState.isAdmin()) {
      <div class="home-header__stats">
        <div class="home-header__stat">
          <span class="home-header__stat-value">{{ adminStats().activeResidents }}</span>
          <span class="home-header__stat-label">Vecinos</span>
        </div>
        <div class="home-header__stat-divider"></div>
        <div class="home-header__stat">
          <span class="home-header__stat-value">{{ adminStats().pendingRegistrations }}</span>
          <span class="home-header__stat-label">Pendientes</span>
        </div>
        <div class="home-header__stat-divider"></div>
        <div class="home-header__stat">
          <span class="home-header__stat-value">{{ adminStats().guards }}</span>
          <span class="home-header__stat-label">Vigilantes</span>
        </div>
      </div>
    }
  </div>

  <ion-content [fullscreen]="true">
    <div class="home-content">

      <!-- === VECINO (y también admin para sus visitas) === -->
      @if (authState.isVecino() || authState.isAdmin()) {

        @if (authState.isAdmin()) {
          <p class="home-section-label">MIS VISITAS</p>
        }

        <!-- Hero: Nueva Visita -->
        <rp-card variant="action" class="home-hero" routerLink="/home/nueva-visita">
          <div class="home-hero__icon">🧑‍💼</div>
          <div class="home-hero__body">
            <h2 class="home-hero__title">Nueva Visita</h2>
            <p class="home-hero__desc">Generá un código QR para tu visitante</p>
          </div>
          <rp-button>Crear visita</rp-button>
        </rp-card>

        <!-- Secundarias -->
        <div class="home-grid">
          <rp-card variant="list-item" routerLink="/home/historial">
            <div class="home-list-icon">📋</div>
            <div class="home-list-body">
              <p class="home-list-title">Historial</p>
              <p class="home-list-sub">Ver visitas pasadas</p>
            </div>
            <span class="home-list-arrow">›</span>
          </rp-card>

          <rp-card variant="list-item" routerLink="/profile">
            <div class="home-list-icon">👤</div>
            <div class="home-list-body">
              <p class="home-list-title">Mi Perfil</p>
              <p class="home-list-sub">Datos de tu cuenta</p>
            </div>
            <span class="home-list-arrow">›</span>
          </rp-card>
        </div>
      }

      <!-- === ADMIN: ZONA ADMINISTRACIÓN === -->
      @if (authState.isAdmin()) {
        <div class="home-divider">
          <span class="home-divider__label">ADMINISTRACIÓN</span>
        </div>

        <div class="home-admin-list">

          <rp-card variant="list-item" routerLink="/admin/registros">
            <div class="home-list-icon">📝</div>
            <div class="home-list-body">
              <p class="home-list-title">Registros pendientes</p>
              <p class="home-list-sub">Aprobar vecinos nuevos</p>
            </div>
            @if (adminStats().pendingRegistrations > 0) {
              <rp-badge status="pending">{{ adminStats().pendingRegistrations }}</rp-badge>
            }
            <span class="home-list-arrow">›</span>
          </rp-card>

          <rp-card variant="list-item" routerLink="/admin/vecinos">
            <div class="home-list-icon">🏘️</div>
            <div class="home-list-body">
              <p class="home-list-title">Vecinos</p>
              <p class="home-list-sub">Gestionar residentes</p>
            </div>
            <span class="home-list-arrow">›</span>
          </rp-card>

          <rp-card variant="list-item" routerLink="/admin/vigilantes">
            <div class="home-list-icon">🛡️</div>
            <div class="home-list-body">
              <p class="home-list-title">Vigilantes</p>
              <p class="home-list-sub">Gestionar guardias</p>
            </div>
            <span class="home-list-arrow">›</span>
          </rp-card>

          <rp-card variant="list-item" routerLink="/admin/registrar-vigilante">
            <div class="home-list-icon">➕</div>
            <div class="home-list-body">
              <p class="home-list-title">Registrar vigilante</p>
              <p class="home-list-sub">Añadir nuevo guardia</p>
            </div>
            <span class="home-list-arrow">›</span>
          </rp-card>

        </div>
      }

      <!-- === VIGILANCIA === -->
      @if (authState.isVigilancia()) {

        <!-- Hero: Escanear QR -->
        <rp-card variant="action" class="home-hero home-hero--dark" routerLink="/home/escanear">
          <div class="home-hero__icon home-hero__icon--dark">📷</div>
          <div class="home-hero__body">
            <h2 class="home-hero__title home-hero__title--light">Escanear QR</h2>
            <p class="home-hero__desc home-hero__desc--light">Registrá la entrada o salida</p>
          </div>
          <rp-button>Abrir cámara</rp-button>
        </rp-card>

        <!-- Bitácora -->
        <rp-card variant="list-item" routerLink="/home/bitacora">
          <div class="home-list-icon">📖</div>
          <div class="home-list-body">
            <p class="home-list-title">Mi Bitácora</p>
            <p class="home-list-sub">Escaneos de este turno</p>
          </div>
          <span class="home-list-arrow">›</span>
        </rp-card>

      }

    </div>
  </ion-content>
</ion-page>
```

- [ ] **Step 3: Reemplazar home.page.scss**

```scss
.home-header {
  background: var(--rp-stone-950);
  padding: calc(16px + var(--ion-safe-area-top, 0px)) var(--rp-page-padding) 20px;
}

.home-header__hello {
  font-family: var(--rp-font);
  font-size: 13px;
  color: var(--rp-stone-500);
  margin: 0;
}

.home-header__name {
  font-family: var(--rp-font);
  font-size: 24px;
  font-weight: 800;
  color: white;
  margin: 2px 0 0;
}

.home-header__stats {
  display: flex;
  align-items: center;
  gap: 16px;
  margin-top: 16px;
  background: rgba(255, 255, 255, 0.06);
  border-radius: var(--rp-radius-lg);
  padding: 12px 16px;
}

.home-header__stat {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 2px;
  flex: 1;
}

.home-header__stat-value {
  font-family: var(--rp-font);
  font-size: 20px;
  font-weight: 800;
  color: white;
}

.home-header__stat-label {
  font-family: var(--rp-font);
  font-size: 10px;
  color: var(--rp-stone-500);
  text-transform: uppercase;
  letter-spacing: 0.06em;
}

.home-header__stat-divider {
  width: 1px;
  height: 32px;
  background: rgba(255, 255, 255, 0.12);
}

.home-content {
  padding: 20px var(--rp-page-padding);
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.home-section-label {
  font-family: var(--rp-font);
  font-size: 10px;
  font-weight: 700;
  color: var(--rp-stone-500);
  letter-spacing: 0.08em;
  margin: 4px 0 0;
}

.home-hero {
  &--dark {
    background: var(--rp-stone-950) !important;
  }
}

.home-hero__icon {
  font-size: 40px;
  width: 72px;
  height: 72px;
  background: var(--rp-primary-tint);
  border-radius: var(--rp-radius-xl);
  display: flex;
  align-items: center;
  justify-content: center;

  &--dark {
    background: rgba(234, 88, 12, 0.2);
  }
}

.home-hero__body {
  text-align: center;
}

.home-hero__title {
  font-family: var(--rp-font);
  font-size: 20px;
  font-weight: 800;
  color: var(--rp-stone-950);
  margin: 0 0 4px;

  &--light {
    color: white;
  }
}

.home-hero__desc {
  font-family: var(--rp-font);
  font-size: 13px;
  color: var(--rp-stone-500);
  margin: 0;

  &--light {
    color: var(--rp-stone-300);
  }
}

.home-grid {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.home-list-icon {
  font-size: 22px;
  width: 44px;
  height: 44px;
  background: var(--rp-primary-tint);
  border-radius: var(--rp-radius-md);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.home-list-body {
  flex: 1;
  min-width: 0;
}

.home-list-title {
  font-family: var(--rp-font);
  font-size: 14px;
  font-weight: 700;
  color: var(--rp-stone-950);
  margin: 0 0 2px;
}

.home-list-sub {
  font-family: var(--rp-font);
  font-size: 12px;
  color: var(--rp-stone-500);
  margin: 0;
}

.home-list-arrow {
  font-size: 20px;
  color: var(--rp-stone-300);
  flex-shrink: 0;
}

.home-divider {
  display: flex;
  align-items: center;
  gap: 12px;
  margin: 8px 0 4px;

  &__label {
    font-family: var(--rp-font);
    font-size: 10px;
    font-weight: 700;
    color: var(--rp-stone-500);
    letter-spacing: 0.08em;
    white-space: nowrap;
  }

  &::before, &::after {
    content: '';
    flex: 1;
    height: 1px;
    background: var(--rp-stone-100);
  }
}

.home-admin-list {
  display: flex;
  flex-direction: column;
  gap: 10px;
}
```

- [ ] **Step 4: Verificar los 3 roles en browser**

```bash
ionic serve
```

Logeate con cada rol y verificá: vecino ve hero "Nueva Visita" + 2 cards, vigilante ve hero "Escanear QR" oscuro, admin ve stats en el header + dual zone con sus visitas + zona de administración.

- [ ] **Step 5: Commit**

```bash
git add src/app/features/home/home.page.ts src/app/features/home/home.page.html src/app/features/home/home.page.scss
git commit -m "feat(home): redesign home with Action-First layout, dual zone for admin role"
```

---

## Task 10: Nueva Visita — Toggle Chips de Horario

**Files:**
- Modify: `src/app/features/home/nueva-visita/nueva-visita.page.html`
- Modify: `src/app/features/home/nueva-visita/nueva-visita.page.scss`
- Modify: `src/app/features/home/nueva-visita/nueva-visita.page.ts` (agregar signal `scheduleType`)

- [ ] **Step 1: Agregar signal en nueva-visita.page.ts**

```typescript
import { signal } from '@angular/core';
import { RpButtonComponent } from '../../../shared/components/rp-button/rp-button.component';
import { RpInputComponent } from '../../../shared/components/rp-input/rp-input.component';

// Dentro del componente:
scheduleType = signal<'open' | 'datetime'>('open');
```

Agregá `RpButtonComponent, RpInputComponent` al array `imports`.

- [ ] **Step 2: Reemplazar nueva-visita.page.html**

```html
<ion-page>

  <div class="nv-header">
    <button class="nv-header__back" (click)="goBack()" type="button">←</button>
    <div>
      <h1 class="nv-header__title">Nueva Visita</h1>
      <p class="nv-header__subtitle">{{ addressLabel() }}</p>
    </div>
  </div>

  <ion-content [fullscreen]="true">
    <form [formGroup]="visitForm" (ngSubmit)="onSubmit()" class="nv-form">

      <rp-input
        label="Nombre del visitante"
        type="text"
        placeholder="Ej. Juan García"
        formControlName="visitorName"
      />

      <!-- Selector de motivo -->
      <label class="nv-select-wrapper">
        <span class="nv-select-label">MOTIVO</span>
        <select class="nv-select" formControlName="reason">
          <option value="" disabled>Seleccioná un motivo</option>
          <option value="visita">Visita personal</option>
          <option value="servicio">Servicio / mantenimiento</option>
          <option value="delivery">Entrega / delivery</option>
          <option value="otro">Otro</option>
        </select>
        <span class="nv-select-arrow">⌄</span>
      </label>

      <!-- Toggle chips de horario -->
      <div class="nv-schedule">
        <p class="nv-schedule__label">TIPO DE HORARIO</p>
        <div class="nv-schedule__chips">
          <button
            type="button"
            class="nv-chip"
            [class.nv-chip--active]="scheduleType() === 'open'"
            (click)="scheduleType.set('open')"
          >Abierto</button>
          <button
            type="button"
            class="nv-chip"
            [class.nv-chip--active]="scheduleType() === 'datetime'"
            (click)="scheduleType.set('datetime')"
          >Fecha / hora</button>
        </div>
      </div>

      @if (scheduleType() === 'datetime') {
        <rp-input
          label="Entrada"
          type="text"
          placeholder="DD/MM/AAAA HH:MM"
          formControlName="entryTime"
        />
        <rp-input
          label="Salida"
          type="text"
          placeholder="DD/MM/AAAA HH:MM"
          formControlName="exitTime"
        />
      }

      <rp-input
        label="Nota (opcional)"
        type="text"
        placeholder="Instrucciones adicionales"
        formControlName="note"
      />

      <rp-button type="submit" [loading]="loading()">
        Generar código QR →
      </rp-button>

    </form>
  </ion-content>
</ion-page>
```

- [ ] **Step 3: Reemplazar nueva-visita.page.scss**

```scss
.nv-header {
  background: var(--rp-stone-950);
  padding: calc(16px + var(--ion-safe-area-top, 0px)) var(--rp-page-padding) 20px;
  display: flex;
  align-items: center;
  gap: 12px;
}

.nv-header__back {
  width: 36px;
  height: 36px;
  background: rgba(255, 255, 255, 0.1);
  border: none;
  border-radius: 10px;
  color: white;
  font-size: 18px;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.nv-header__title {
  font-family: var(--rp-font);
  font-size: 18px;
  font-weight: 800;
  color: white;
  margin: 0 0 2px;
}

.nv-header__subtitle {
  font-family: var(--rp-font);
  font-size: 12px;
  color: var(--rp-stone-500);
  margin: 0;
}

.nv-form {
  padding: 20px var(--rp-page-padding);
  display: flex;
  flex-direction: column;
  gap: 14px;
}

.nv-select-wrapper {
  background: var(--rp-surface);
  border: 1.5px solid var(--rp-stone-100);
  border-radius: var(--rp-radius-md);
  min-height: 56px;
  padding: 10px 14px;
  display: flex;
  flex-direction: column;
  position: relative;
}

.nv-select-label {
  font-family: var(--rp-font);
  font-size: 10px;
  font-weight: 600;
  color: var(--rp-stone-500);
  text-transform: uppercase;
  letter-spacing: 0.06em;
  margin-bottom: 4px;
}

.nv-select {
  border: none;
  outline: none;
  background: transparent;
  font-family: var(--rp-font);
  font-size: 15px;
  color: var(--rp-stone-950);
  appearance: none;
  padding-right: 24px;
}

.nv-select-arrow {
  position: absolute;
  right: 14px;
  bottom: 14px;
  color: var(--rp-stone-300);
  pointer-events: none;
}

.nv-schedule__label {
  font-family: var(--rp-font);
  font-size: 10px;
  font-weight: 600;
  color: var(--rp-stone-500);
  text-transform: uppercase;
  letter-spacing: 0.06em;
  margin: 0 0 8px;
}

.nv-schedule__chips {
  display: flex;
  gap: 8px;
}

.nv-chip {
  flex: 1;
  height: 40px;
  border: 1.5px solid var(--rp-stone-100);
  border-radius: var(--rp-radius-full);
  background: var(--rp-surface);
  font-family: var(--rp-font);
  font-size: 13px;
  font-weight: 600;
  color: var(--rp-stone-700);
  cursor: pointer;
  transition: all 0.15s ease;

  &--active {
    background: var(--rp-primary-tint);
    border-color: var(--rp-primary);
    color: var(--rp-primary);
  }
}
```

- [ ] **Step 4: Verificar toggle chips**

```bash
ionic serve
```

Navegá a Nueva Visita. Verificá: chips "Abierto" y "Fecha/hora" funcionan, los inputs de fecha/hora aparecen y desaparecen correctamente.

- [ ] **Step 5: Commit**

```bash
git add src/app/features/home/nueva-visita/
git commit -m "feat(nueva-visita): redesign with schedule toggle chips replacing checkbox"
```

---

## Task 11: Detalle Visita / QR

**Files:**
- Modify: `src/app/features/home/detalle-visita/detalle-visita.page.html`
- Modify: `src/app/features/home/detalle-visita/detalle-visita.page.scss`
- Modify: `src/app/features/home/detalle-visita/detalle-visita.page.ts` (agregar imports)

- [ ] **Step 1: Agregar imports en detalle-visita.page.ts**

```typescript
import { RpButtonComponent } from '../../../shared/components/rp-button/rp-button.component';
import { RpBadgeComponent } from '../../../shared/components/rp-badge/rp-badge.component';
```

Agregá ambos al array `imports` del componente.

- [ ] **Step 2: Reemplazar detalle-visita.page.html**

```html
<ion-page>

  <div class="dv-header">
    <button class="dv-header__back" (click)="goBack()" type="button">←</button>
    <div>
      <h1 class="dv-header__title">Detalle de Visita</h1>
    </div>
    <rp-badge [status]="visit()?.status" />
  </div>

  <ion-content [fullscreen]="true">
    <div class="dv-content">

      <!-- Card QR -->
      <div class="dv-qr-card">
        <p class="dv-qr-card__hint">Mostrá este código en la entrada</p>
        <div class="dv-qr-container">
          <!-- El QR ya existe como componente en el proyecto — lo mantenemos -->
          <qr-code [value]="qrValue()" [size]="220" />
        </div>
        <h2 class="dv-qr-card__visitor">{{ visit()?.visitorName }}</h2>
        <p class="dv-qr-card__detail">
          {{ visit()?.reason }} · {{ visit()?.formattedDate }}
        </p>
      </div>

      <!-- Advertencia ID -->
      <div class="dv-id-warning">
        <span class="dv-id-warning__icon">⚠️</span>
        <p class="dv-id-warning__text">
          Se requiere <strong>INE</strong>, <strong>Licencia</strong> o <strong>Pasaporte</strong> para ingresar
        </p>
      </div>

      <!-- CTA Compartir -->
      <rp-button (click)="shareQr()">↗ Compartir QR</rp-button>

      <!-- Cancelar (solo si pending y es el dueño) -->
      @if (visit()?.status === 'pending' && canCancel()) {
        <rp-button variant="danger" [loading]="cancelling()" (click)="cancelVisit()">
          Cancelar visita
        </rp-button>
      }

    </div>
  </ion-content>
</ion-page>
```

- [ ] **Step 3: Reemplazar detalle-visita.page.scss**

```scss
.dv-header {
  background: var(--rp-stone-950);
  padding: calc(16px + var(--ion-safe-area-top, 0px)) var(--rp-page-padding) 20px;
  display: flex;
  align-items: center;
  gap: 12px;
}

.dv-header__back {
  width: 36px;
  height: 36px;
  background: rgba(255, 255, 255, 0.1);
  border: none;
  border-radius: 10px;
  color: white;
  font-size: 18px;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.dv-header__title {
  font-family: var(--rp-font);
  font-size: 18px;
  font-weight: 800;
  color: white;
  margin: 0;
  flex: 1;
}

.dv-content {
  padding: 20px var(--rp-page-padding);
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.dv-qr-card {
  background: var(--rp-surface);
  border-radius: var(--rp-radius-xl);
  padding: 24px 20px;
  box-shadow: var(--rp-shadow-card);
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  gap: 12px;
}

.dv-qr-card__hint {
  font-family: var(--rp-font);
  font-size: 12px;
  color: var(--rp-stone-500);
  margin: 0;
}

.dv-qr-container {
  min-width: 200px;
  min-height: 200px;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 8px;
}

.dv-qr-card__visitor {
  font-family: var(--rp-font);
  font-size: 18px;
  font-weight: 800;
  color: var(--rp-stone-950);
  margin: 0;
}

.dv-qr-card__detail {
  font-family: var(--rp-font);
  font-size: 13px;
  color: var(--rp-stone-500);
  margin: 0;
}

.dv-id-warning {
  background: var(--rp-primary-tint);
  border: 1px solid #fed7aa;
  border-radius: var(--rp-radius-lg);
  padding: 12px 14px;
  display: flex;
  align-items: flex-start;
  gap: 10px;
}

.dv-id-warning__icon {
  font-size: 16px;
  flex-shrink: 0;
}

.dv-id-warning__text {
  font-family: var(--rp-font);
  font-size: 13px;
  color: var(--rp-primary-dark);
  margin: 0;
  line-height: 1.5;
}
```

- [ ] **Step 4: Verificar en browser**

```bash
ionic serve
```

Navegá a una visita pendiente. Verificá: badge de estado en header, QR mínimo 200px, advertencia ID en naranja, botón "Cancelar" solo en status pending.

- [ ] **Step 5: Commit**

```bash
git add src/app/features/home/detalle-visita/
git commit -m "feat(detalle-visita): redesign QR detail with ID warning, share CTA, conditional cancel"
```

---

## Task 12: Escanear Visita (Vigilancia) — Dark UI + Viewfinder

**Files:**
- Modify: `src/app/features/home/escanear-visita/escanear-visita.page.html`
- Modify: `src/app/features/home/escanear-visita/escanear-visita.page.scss`
- Modify: `src/app/features/home/escanear-visita/escanear-visita.page.ts` (agregar imports y signal idType)

- [ ] **Step 1: Agregar en escanear-visita.page.ts**

```typescript
import { signal } from '@angular/core';
import { RpButtonComponent } from '../../../shared/components/rp-button/rp-button.component';
import { RpBadgeComponent } from '../../../shared/components/rp-badge/rp-badge.component';

// Dentro del componente:
idType = signal<'ine' | 'passport' | 'license'>('ine');
hasVehicle = signal(false);
```

Agregá `RpButtonComponent, RpBadgeComponent` al array `imports`.

- [ ] **Step 2: Reemplazar escanear-visita.page.html**

```html
<ion-page>
  <ion-content [fullscreen]="true" [scrollY]="false">
    <div class="scanner">

      <!-- Viewfinder area -->
      <div class="scanner__viewfinder-area">
        <div class="scanner__viewfinder">
          <!-- Esquinas naranjas -->
          <div class="scanner__corner scanner__corner--tl"></div>
          <div class="scanner__corner scanner__corner--tr"></div>
          <div class="scanner__corner scanner__corner--bl"></div>
          <div class="scanner__corner scanner__corner--br"></div>
          <!-- Línea de escaneo animada -->
          <div class="scanner__line"></div>
        </div>
        <p class="scanner__hint">Apuntá la cámara al código QR del visitante</p>
      </div>

      <!-- Panel inferior: aparece al detectar QR -->
      @if (detectedVisit()) {
        <div class="scanner__panel">

          <div class="scanner__visit-info">
            <div class="scanner__visit-header">
              <h2 class="scanner__visitor-name">{{ detectedVisit()?.visitorName }}</h2>
              <rp-badge [status]="detectedVisit()?.status" />
            </div>
            <p class="scanner__visit-detail">
              {{ detectedVisit()?.residentAddress }} · {{ detectedVisit()?.reason }}
            </p>
          </div>

          <!-- Chips tipo de identificación -->
          <div class="scanner__id-section">
            <p class="scanner__id-label">TIPO DE IDENTIFICACIÓN</p>
            <div class="scanner__id-chips">
              <button
                type="button"
                class="scanner__chip"
                [class.scanner__chip--active]="idType() === 'ine'"
                (click)="idType.set('ine')"
              >INE</button>
              <button
                type="button"
                class="scanner__chip"
                [class.scanner__chip--active]="idType() === 'passport'"
                (click)="idType.set('passport')"
              >Pasaporte</button>
              <button
                type="button"
                class="scanner__chip"
                [class.scanner__chip--active]="idType() === 'license'"
                (click)="idType.set('license')"
              >Licencia</button>
            </div>
          </div>

          <!-- Toggle vehículo -->
          <label class="scanner__vehicle-toggle">
            <span class="scanner__vehicle-label">¿Con vehículo?</span>
            <input
              type="checkbox"
              [checked]="hasVehicle()"
              (change)="hasVehicle.set(!hasVehicle())"
              style="accent-color: var(--rp-primary);"
            />
          </label>

          @if (hasVehicle()) {
            <div class="scanner__plate-input">
              <label class="scanner__plate-label">PLACA</label>
              <input
                class="scanner__plate-field"
                type="text"
                placeholder="ABC-1234"
                [(ngModel)]="plateNumber"
              />
            </div>
          }

          <!-- CTA -->
          <rp-button [loading]="registering()" (click)="registerAccess()">
            @if (detectedVisit()?.status === 'pending' || detectedVisit()?.status === 'used') {
              ✓ Registrar {{ detectedVisit()?.status === 'pending' ? 'entrada' : 'salida' }}
            }
          </rp-button>

        </div>
      }

    </div>
  </ion-content>
</ion-page>
```

- [ ] **Step 3: Reemplazar escanear-visita.page.scss**

```scss
.scanner {
  height: 100%;
  background: #111110;
  display: flex;
  flex-direction: column;
}

.scanner__viewfinder-area {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 24px;
  padding: 40px var(--rp-page-padding);
  padding-top: calc(40px + var(--ion-safe-area-top, 0px));
}

.scanner__viewfinder {
  width: 240px;
  height: 240px;
  position: relative;
}

.scanner__corner {
  position: absolute;
  width: 28px;
  height: 28px;
  border-color: var(--rp-primary);
  border-style: solid;
  border-width: 0;

  &--tl {
    top: 0; left: 0;
    border-top-width: 3px;
    border-left-width: 3px;
    border-radius: 4px 0 0 0;
  }

  &--tr {
    top: 0; right: 0;
    border-top-width: 3px;
    border-right-width: 3px;
    border-radius: 0 4px 0 0;
  }

  &--bl {
    bottom: 0; left: 0;
    border-bottom-width: 3px;
    border-left-width: 3px;
    border-radius: 0 0 0 4px;
  }

  &--br {
    bottom: 0; right: 0;
    border-bottom-width: 3px;
    border-right-width: 3px;
    border-radius: 0 0 4px 0;
  }
}

.scanner__line {
  position: absolute;
  left: 8px;
  right: 8px;
  height: 2px;
  background: var(--rp-primary);
  top: 50%;
  animation: scanner-sweep 2s ease-in-out infinite;
  box-shadow: 0 0 8px rgba(234, 88, 12, 0.6);
}

@keyframes scanner-sweep {
  0%   { top: 10%; opacity: 1; }
  50%  { top: 90%; opacity: 1; }
  100% { top: 10%; opacity: 1; }
}

.scanner__hint {
  font-family: var(--rp-font);
  font-size: 13px;
  color: rgba(255, 255, 255, 0.5);
  text-align: center;
  margin: 0;
}

.scanner__panel {
  background: var(--rp-surface);
  border-radius: var(--rp-radius-xl) var(--rp-radius-xl) 0 0;
  padding: 20px var(--rp-page-padding) calc(20px + var(--ion-safe-area-bottom, 0px));
  display: flex;
  flex-direction: column;
  gap: 16px;
  box-shadow: 0 -4px 24px rgba(0, 0, 0, 0.3);
}

.scanner__visit-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  margin-bottom: 4px;
}

.scanner__visitor-name {
  font-family: var(--rp-font);
  font-size: 18px;
  font-weight: 800;
  color: var(--rp-stone-950);
  margin: 0;
}

.scanner__visit-detail {
  font-family: var(--rp-font);
  font-size: 13px;
  color: var(--rp-stone-500);
  margin: 0;
}

.scanner__id-label {
  font-family: var(--rp-font);
  font-size: 10px;
  font-weight: 600;
  color: var(--rp-stone-500);
  text-transform: uppercase;
  letter-spacing: 0.06em;
  margin: 0 0 8px;
}

.scanner__id-chips {
  display: flex;
  gap: 8px;
}

.scanner__chip {
  flex: 1;
  height: 38px;
  border: 1.5px solid var(--rp-stone-100);
  border-radius: var(--rp-radius-full);
  background: var(--rp-bg);
  font-family: var(--rp-font);
  font-size: 12px;
  font-weight: 600;
  color: var(--rp-stone-700);
  cursor: pointer;
  transition: all 0.15s ease;

  &--active {
    background: var(--rp-primary-tint);
    border-color: var(--rp-primary);
    color: var(--rp-primary);
  }
}

.scanner__vehicle-toggle {
  display: flex;
  align-items: center;
  justify-content: space-between;
  cursor: pointer;
}

.scanner__vehicle-label {
  font-family: var(--rp-font);
  font-size: 14px;
  font-weight: 600;
  color: var(--rp-stone-950);
}

.scanner__plate-input {
  background: var(--rp-bg);
  border: 1.5px solid var(--rp-stone-100);
  border-radius: var(--rp-radius-md);
  padding: 10px 14px;
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.scanner__plate-label {
  font-family: var(--rp-font);
  font-size: 10px;
  font-weight: 600;
  color: var(--rp-stone-500);
  text-transform: uppercase;
  letter-spacing: 0.06em;
}

.scanner__plate-field {
  border: none;
  outline: none;
  background: transparent;
  font-family: var(--rp-font);
  font-size: 15px;
  font-weight: 700;
  color: var(--rp-stone-950);
  letter-spacing: 0.1em;
  text-transform: uppercase;

  &::placeholder {
    color: var(--rp-stone-300);
    font-weight: 400;
    text-transform: none;
    letter-spacing: normal;
  }
}
```

- [ ] **Step 4: Verificar en browser**

```bash
ionic serve
```

Logeate como vigilante. Navegá a Escanear QR. Verificá: fondo oscuro, viewfinder con esquinas naranjas y línea animada, panel inferior con chips de ID y toggle vehículo.

- [ ] **Step 5: Commit**

```bash
git add src/app/features/home/escanear-visita/
git commit -m "feat(escanear-visita): dark scanner UI with orange viewfinder and ID type chips"
```

---

## Task 13: Historial de Visitas — Filtro Segmentado + Borde Lateral

**Files:**
- Modify: `src/app/features/home/historial-visitas/historial-visitas.page.html`
- Modify: `src/app/features/home/historial-visitas/historial-visitas.page.scss`
- Modify: `src/app/features/home/historial-visitas/historial-visitas.page.ts` (agregar signal activeFilter + lógica expand)

- [ ] **Step 1: Agregar signals en historial-visitas.page.ts**

```typescript
import { signal, computed } from '@angular/core';
import { RpBadgeComponent } from '../../../shared/components/rp-badge/rp-badge.component';
import { RpButtonComponent } from '../../../shared/components/rp-button/rp-button.component';

// Dentro del componente:
activeFilter = signal<'all' | 'pending' | 'past'>('all');
expandedVisitId = signal<string | null>(null);
cancellingId = signal<string | null>(null);

filteredVisits = computed(() => {
  const filter = this.activeFilter();
  const visits = this.visits();
  if (filter === 'all') return visits;
  if (filter === 'pending') return visits.filter(v => v.status === 'pending' || v.status === 'used');
  return visits.filter(v => v.status === 'finished' || v.status === 'expired' || v.status === 'cancelled');
});

toggleExpand(id: string): void {
  this.expandedVisitId.update(current => current === id ? null : id);
}
```

Agregá `RpBadgeComponent, RpButtonComponent` al array `imports`.

- [ ] **Step 2: Reemplazar historial-visitas.page.html**

```html
<ion-page>

  <div class="hist-header">
    <button class="hist-header__back" (click)="goBack()" type="button">←</button>
    <h1 class="hist-header__title">Historial</h1>
  </div>

  <!-- Filtro segmentado -->
  <div class="hist-filter">
    <button
      class="hist-filter__btn"
      [class.hist-filter__btn--active]="activeFilter() === 'all'"
      (click)="activeFilter.set('all')"
    >Todas</button>
    <button
      class="hist-filter__btn"
      [class.hist-filter__btn--active]="activeFilter() === 'pending'"
      (click)="activeFilter.set('pending')"
    >Pendientes</button>
    <button
      class="hist-filter__btn"
      [class.hist-filter__btn--active]="activeFilter() === 'past'"
      (click)="activeFilter.set('past')"
    >Pasadas</button>
  </div>

  <ion-content [fullscreen]="true">
    <ion-refresher slot="fixed" (ionRefresh)="refresh($event)">
      <ion-refresher-content />
    </ion-refresher>

    <div class="hist-list">
      @for (visit of filteredVisits(); track visit.id) {
        <div
          class="hist-item"
          [class]="'hist-item hist-item--' + visit.status"
          (click)="toggleExpand(visit.id)"
        >
          <div class="hist-item__main">
            <div class="hist-item__info">
              <h3 class="hist-item__name">{{ visit.visitorName }}</h3>
              <p class="hist-item__meta">
                {{ visit.reason }} · {{ visit.formattedDate }}
              </p>
              @if (visit.idType) {
                <p class="hist-item__id">{{ visit.idType }}</p>
              }
            </div>
            <rp-badge [status]="visit.status" />
          </div>

          <!-- Acción expandida (solo pending, solo vecino/admin) -->
          @if (expandedVisitId() === visit.id && visit.status === 'pending' && canCancelVisit()) {
            <div class="hist-item__actions">
              <rp-button
                variant="danger"
                [loading]="cancellingId() === visit.id"
                (click)="cancelVisit(visit.id, $event)"
              >
                Cancelar visita
              </rp-button>
            </div>
          }
        </div>
      }

      @if (filteredVisits().length === 0) {
        <div class="hist-empty">
          <p class="hist-empty__text">No hay visitas para mostrar</p>
        </div>
      }
    </div>
  </ion-content>
</ion-page>
```

- [ ] **Step 3: Agregar método `cancelVisit` en historial-visitas.page.ts**

El método recibe el id de la visita y el evento click para evitar propagación:

```typescript
async cancelVisit(id: string, event: Event): Promise<void> {
  event.stopPropagation();
  this.cancellingId.set(id);
  try {
    await this.visitsService.cancelVisit(id);
    // Recargá la lista desde el servicio existente
    await this.loadVisits();
  } finally {
    this.cancellingId.set(null);
    this.expandedVisitId.set(null);
  }
}
```

> ⚠️ Adaptá `this.visitsService.cancelVisit(id)` y `this.loadVisits()` al nombre real del servicio y método que ya existe en el componente. No cambies la lógica del servicio, solo el template y los signals.

- [ ] **Step 4: Reemplazar historial-visitas.page.scss**

```scss
.hist-header {
  background: var(--rp-stone-950);
  padding: calc(16px + var(--ion-safe-area-top, 0px)) var(--rp-page-padding) 16px;
  display: flex;
  align-items: center;
  gap: 12px;
}

.hist-header__back {
  width: 36px;
  height: 36px;
  background: rgba(255, 255, 255, 0.1);
  border: none;
  border-radius: 10px;
  color: white;
  font-size: 18px;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.hist-header__title {
  font-family: var(--rp-font);
  font-size: 18px;
  font-weight: 800;
  color: white;
  margin: 0;
}

.hist-filter {
  display: flex;
  gap: 0;
  background: var(--rp-stone-950);
  padding: 0 var(--rp-page-padding) 16px;
  border-bottom: 1px solid rgba(255, 255, 255, 0.08);
}

.hist-filter__btn {
  flex: 1;
  height: 36px;
  border: none;
  background: rgba(255, 255, 255, 0.08);
  font-family: var(--rp-font);
  font-size: 12px;
  font-weight: 600;
  color: var(--rp-stone-300);
  cursor: pointer;
  transition: all 0.15s ease;

  &:first-child {
    border-radius: var(--rp-radius-full) 0 0 var(--rp-radius-full);
  }

  &:last-child {
    border-radius: 0 var(--rp-radius-full) var(--rp-radius-full) 0;
  }

  &--active {
    background: var(--rp-primary);
    color: white;
  }
}

.hist-list {
  padding: 16px var(--rp-page-padding);
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.hist-item {
  background: var(--rp-surface);
  border-radius: var(--rp-radius-lg);
  box-shadow: var(--rp-shadow-card);
  overflow: hidden;
  border-left: 4px solid transparent;
  cursor: pointer;

  &--pending, &--used {
    border-left-color: var(--rp-primary);
  }

  &--finished, &--active {
    border-left-color: var(--rp-success);
  }

  &--cancelled, &--expired, &--suspended {
    border-left-color: var(--rp-stone-300);
  }
}

.hist-item__main {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 12px;
  padding: 14px 14px 14px 12px;
}

.hist-item__info {
  flex: 1;
  min-width: 0;
}

.hist-item__name {
  font-family: var(--rp-font);
  font-size: 14px;
  font-weight: 700;
  color: var(--rp-stone-950);
  margin: 0 0 3px;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.hist-item__meta {
  font-family: var(--rp-font);
  font-size: 12px;
  color: var(--rp-stone-500);
  margin: 0 0 2px;
}

.hist-item__id {
  font-family: var(--rp-font);
  font-size: 11px;
  color: var(--rp-stone-300);
  margin: 0;
}

.hist-item__actions {
  padding: 0 12px 12px;
}

.hist-empty {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 40px;
}

.hist-empty__text {
  font-family: var(--rp-font);
  font-size: 14px;
  color: var(--rp-stone-300);
  margin: 0;
}
```

- [ ] **Step 5: Verificar filtro y borde lateral**

```bash
ionic serve
```

Navegá a Historial. Verificá: filtros "Todas / Pendientes / Pasadas" funcionan, cada item tiene borde lateral de color semántico (naranja=pendiente, verde=completada, gris=cancelada), tocar un item pendiente muestra el botón "Cancelar visita".

- [ ] **Step 6: Commit final**

```bash
git add src/app/features/home/historial-visitas/
git commit -m "feat(historial): segmented filter, semantic left border, inline cancel on expand"
```

---

## Spec Coverage Check

| Requisito del spec | Task |
|---|---|
| Plus Jakarta Sans, 5 niveles tipográficos | Task 1 |
| CSS tokens paleta Terracota | Task 1 |
| Dark mode via @media, eliminar dark.always.css | Task 1 |
| RpButtonComponent 3 variantes + loading | Task 2 |
| RpInputComponent ControlValueAccessor + focus ring | Task 3 |
| RpCardComponent action/list-item | Task 4 |
| RpBadgeComponent 7 status + 3 roles | Task 5 |
| Welcome: hero oscuro, logo naranja, 2 botones | Task 6 |
| Login: inputs custom, toggle password, recordar | Task 7 |
| Register: 3 pasos con progress bar | Task 8 |
| Home Vecino: hero Nueva Visita + 2 secundarias | Task 9 |
| Home Vigilancia: hero Escanear QR oscuro | Task 9 |
| Home Admin: stats en header + dual zone | Task 9 |
| Nueva Visita: toggle chips Abierto/Fecha | Task 10 |
| Detalle Visita: QR 200px+, advertencia ID, compartir | Task 11 |
| Escanear: dark UI, viewfinder naranja animado, chips ID | Task 12 |
| Historial: filtro segmentado, borde lateral semántico | Task 13 |
| Sin swipe en historial (interfiere con scroll) | Task 13 — botón inline ✓ |
| Touch targets ≥ 48px | Todos los botones: height: 48px ✓ |
| Ionic como shell únicamente | Sin IonButton/IonInput en ningún componente visual ✓ |
| Angular standalone | Todos los componentes: standalone: true ✓ |
